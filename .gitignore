# OSM / tile data — never commit, these are large + regenerable
*.osm.pbf
*.pmtiles
*.mbtiles
data/raw/
# Planetiler auxiliary downloads (lake / natural-earth / water)
data/sources/
# Planetiler tile_weights / layerstats
data/*.tsv.gz
valhalla_tiles/
valhalla_tiles.tar
# Valhalla graph + working pbf (Phase 2)
services/routing/custom_files/
# Geocoder SQLite index (Phase 3) — build artifact
services/geocoder/data/
*.db

# Self-hosted glyph fonts (downloaded via `make fonts`)
services/tiles/fonts/

# Tooling state
.claude/

# Env
.env
.env.local

# Python
__pycache__/
*.pyc
.venv/
venv/

# Editor / OS
.DS_Store
.idea/
.vscode/

# bug-hunting harness (local deps + artifacts)
harness/node_modules/
harness/package.json
harness/package-lock.json
harness/*.png

# Photon search index (downloaded, ~0.5-1GB)
services/geocoder-photon/data/
